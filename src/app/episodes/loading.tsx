import { StandardLoading } from "@/components/route/StandardLoading";

export default function Loading() {
  return <StandardLoading title="Episodes" subtitle="Loading episodes…" variant="text" />;
}
