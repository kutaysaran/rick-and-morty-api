import { StandardLoading } from "@/components/route/StandardLoading";

export default function Loading() {
  return (
    <StandardLoading filters title="Characters" subtitle="Loading characters…" variant="image" />
  );
}
